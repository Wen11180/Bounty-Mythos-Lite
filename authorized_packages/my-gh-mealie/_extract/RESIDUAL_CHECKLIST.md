# Residual checklist — my-gh-mealie (optional)

Static package trial expected **refute** (guards present).

| ID | Question | Static status |
| --- | --- | --- |
| ML-R1 | Recipe load group-scoped before body? | **held** |
| ML-R2 | Update gated by can_update (owner/lock/household)? | **held** |
| ML-R3 | Delete gated by can_delete ownership (or admin)? | **held** |
| ML-R4 | Lock/unlock owner-only? | **held** (modeled/docs) |
| ML-R5 | Bulk patch/delete + last-made bypass path? | **not checked this package** |

Live residual: researcher-owned loopback only.
