# V0 Acceptance Audit

Date: 2026-07-07

This audit checks the current implementation against `docs/final-proposal-development-plan.md`.
It is intentionally conservative: an item is marked complete only when there is direct code or
test evidence in the current project.

## Scope

V0 covers the local source audit workbench only:

`local repo + scope file -> source audit scan -> pipeline run -> artifact/provenance -> validation workspace -> report preview -> manual observation -> claim review -> finding candidate`

Out of scope for V0:

- Public target scanning.
- Automatic exploit or payload execution.
- Automatic report submission.
- Treating scanner, LLM, or imported third-party output as a confirmed vulnerability.
- Saving raw secrets, cookies, tokens, authorization headers, or real user data.

## Acceptance Matrix

| Requirement | Status | Evidence | Remaining Work |
| --- | --- | --- | --- |
| Local repo must be allowlisted before scan | Complete | `apps/api/tests/test_source_audit.py` covers unallowlisted repo blocking and no pipeline run creation. | None for V0. |
| Missing or unauthorized local repo fails closed | Complete | `test_run_source_audit_blocks_unallowlisted_repo_before_semgrep` proves Semgrep is not called after scope block. | Add a dedicated API test for non-existent repo if the API should expose a distinct error. |
| Scan reads local source metadata only | Complete | Source audit tests assert raw source snippets like `send_file(file_id)` do not appear in report, payload, detail, or candidate responses. | Keep expanding leak checks as new artifact types are added. |
| Intake identifies supported stacks | Complete | Source audit tests cover Python, TypeScript/JavaScript, Go, FastAPI, Next.js, and manifest discovery. | Add more framework fixtures only when needed by real targets. |
| Dependency handling is manifest summary only | Complete | Tests assert manifest counts and package summaries without CVE promotion. | None for V0. |
| Semgrep failure or absence is non-fatal | Complete | `run_semgrep` tests cover JSON normalization and wrapper behavior; source audit supports skipped status. | Keep CLI environment failures covered when packaging. |
| CodeQL remains skipped/not configured unless injected | Complete | Tests assert injected and skipped CodeQL states. | Real CodeQL execution belongs outside the V0 acceptance gate. |
| Studio run uses imported policy artifact when present | Complete | `test_studio_run_uses_imported_policy_artifact_for_audit_hash` imports separate scope, policy, and code artifacts, runs the workspace audit, and asserts the persisted pipeline run hashes the policy text rather than the scope file. | Extend policy display only if the Studio UI needs a dedicated policy review surface. |
| Studio mission summary exposes software-workbench state | Complete for the local read-only mission contract and desktop workbench mapper | `test_studio_mission_summary_exposes_desktop_workbench_state` verifies `/mythos/studio/workspaces/mission` returns the latest run id, Scope Guard status, required A+B artifact coverage, a read-only research-loop stage summary, review-only agent queue work orders, Top 1-5 candidate summaries, submission-blocked quality gates, blocked actions, and safe next actions for a desktop/local client without raw repo paths, code snippets, validation execution, fuzzer execution, or report-submission controls. `test_studio_mission_summary_exposes_strategy_as_advisory_context` verifies strategy artifacts appear only as advisory mission context while validation and submission remain blocked. Web tests cover `getStudioWorkspaceMission`, `toStudioMissionPanel`, and the `/studio` Mission control panel consuming that local mission summary without unsafe calls. | Extend when the desktop shell needs richer task queue state. |
| Imported API and HAR artifacts enrich Studio candidates safely | Complete for OpenAPI route context, HAR traffic context, and template-route matching | `test_studio_candidates_include_imported_api_surface_context` imports OpenAPI route metadata and attaches only normalized route/operation facts to candidates. `test_studio_candidates_include_imported_har_context_without_secrets` imports HAR traffic, attaches only method/path facts as `advisory_only`, and asserts HAR filenames, query tokens, authorization headers, cookies, and secret values do not leak. `test_studio_candidates_match_imported_api_and_har_template_routes` verifies `/files/{id}/export` and `/files/123/export` match the code candidate `/files/{file_id}/export` without attaching unrelated `/admin/users` traffic. | Extend HAR status summaries only when needed by real authorized inputs. |
| Studio A+B candidate quality can be benchmarked locally | Complete for saved candidate JSON, reviewable expectation templates, expectation files, workspace run benchmarks, and synthetic fixture corpus | `tests/test_intelligence_benchmark.py` covers `build_studio_expectations_template`, `evaluate_studio_candidates`, `python -m app studio-eval-template`, and `python -m app studio-eval`; `test_studio_benchmark_fixtures_pass_quality_gate` verifies `tests/fixtures/studio_benchmarks/ab_agent_tool_authz_*`, `ab_file_export_*`, `ab_money_flow_*`, `ab_rag_document_authz_*`, `ab_role_boundary_*`, and `ab_webhook_egress_*` as reusable, self-describing A+B corpus entries with fixture metadata for risk family, authorized inputs, and safety boundaries plus scope, policy, code, API, HAR, optional advisory SARIF/SBOM/fuzzing where relevant, security invariant, impact rationale, repair guidance, regression test, refutation, provenance review, duplicate-risk cap, safety blockers, risk-family-specific evidence/refutation keywords, and forbidden-text checks; `test_studio_run_lists_candidates_and_exports_submission_blocked_report` covers `/mythos/studio/workspaces/benchmarks/template` persisting a draft expectation template and `/mythos/studio/workspaces/benchmarks/run` persisting a benchmark result into the local workspace; Web helper tests cover both frontend bridges. The generated template keeps scope, policy, code, API, and HAR artifacts required, marks affected code-path, security-invariant, impact-rationale, repair-guidance, regression-test, refutation-status, refutation-review, policy-risk, policy-review, evidence-review, provenance-review, deduplication-review, and validation-review coverage as required for human review, caps duplicate and policy risk, and the evaluator emits structured artifact and keyword evidence gaps; the evaluator also requires non-empty expected candidates, expected route/vulnerability matches, candidate and expected-candidate sets capped at 1-5 high-signal candidates, evidence needs, false-positive checks, optional expected evidence/false-positive keyword coverage with punctuation-insensitive matching, safe validation plans that avoid live outbound, production, and real-user-data language, validation review with execution blocked, submission-blocked report readiness with a human next action that does not ask for report submission or validation execution, required artifact coverage, advisory-only scanner/dependency/fuzzing/strategy signals, safety blockers, and case-insensitive forbidden-text absence. | Add more fixture packs for real authorized or safely redacted target patterns after the synthetic corpus expands. |
| Imported scanner, dependency, fuzzing-plan, and strategy artifacts remain advisory in Studio candidates | Complete for SARIF endpoint context, SBOM dependency context, non-executing fuzzing-plan context, strategy-note context, benchmark advisory-signal gates, and desktop optional-context import | `test_studio_candidates_include_imported_sarif_scanner_context_as_advisory` imports a SARIF scanner artifact, attaches only normalized `scanner_signal` route metadata to candidates, marks it `advisory_only`, asserts scanner filenames, bearer tokens, and raw authorization text do not leak, and verifies the generated benchmark template/run keeps SARIF as an advisory artifact requirement. `test_studio_candidates_include_imported_sbom_dependency_context_as_advisory` imports a CycloneDX SBOM artifact, attaches only normalized `dependency_signal` package metadata to candidates, marks it `advisory_only`, and asserts SBOM filenames, bearer tokens, and raw descriptions do not leak. `test_studio_candidates_include_imported_fuzzing_plan_context_as_advisory` imports a plan-only fuzzing artifact, attaches only normalized `fuzzing_signal` target metadata to candidates, marks it `advisory_only`, asserts fuzzing filenames, bearer tokens, cookies, and secret values do not leak, and verifies generated benchmark templates keep fuzzing as an advisory artifact requirement. `test_studio_candidates_include_imported_strategy_context_as_advisory` imports Markdown strategy notes, attaches only `focus`, `risk_family`, and one safe `note` as a `strategy_signal`, marks it `advisory_only`, exports a strategy advisory label, and asserts filenames, bearer tokens, and secret values do not leak. `test_studio_fuzzing_surface_facts_ignore_executable_plans` asserts executable fuzzing plans produce no Studio signal. Web tests assert Strategy and Fuzzing plan files are optional local context in the Studio workbench and do not expose execution or submission actions. `tests/test_intelligence_benchmark.py` also fails candidate quality when expected SARIF/SBOM/fuzzing/strategy artifacts are not represented by advisory-only scanner/dependency/fuzzing/strategy signals. | Extend to more scanner, dependency, fuzzing-plan, and strategy-note formats only when they are needed by real authorized inputs. |
| Findings are hypotheses, not verified vulnerabilities | Complete | Finding JSON and report preview tests assert `unverified_hypothesis`, human review requirements, readiness blockers, `refutation_status`, `false_positive_checks`, `priority_score`, and `ranking_reasons`. | Keep later Phase 3 work advisory unless a human promotes evidence. |
| Studio candidate report readiness stays submission-blocked | Complete for candidate cards and evidence-review gating | `test_studio_run_lists_candidates_and_exports_submission_blocked_report` asserts candidate `report_readiness.status == submission_blocked`, `report_submission_allowed == false`, a review-only next action, and `evidence_review.status == needs_human_review` with required human review items; Web tests assert candidate cards surface report readiness without adding submission controls. | Keep future report actions behind the existing human evidence and redaction gates. |
| Studio report draft carries A+B context safely | Complete for exported JSON and Markdown drafts | `test_studio_run_lists_candidates_and_exports_submission_blocked_report` asserts exported reports include `studio_context.required_artifacts` for scope, policy, code, API, and HAR; include normalized API/HAR route facts; carry candidate summary, ranking reasons, report readiness, refutation review, policy review, evidence review, provenance review, deduplication review, validation review, evidence needs, false-positive checks, evidence gaps, policy-risk summary, safe validation plan, safety blockers, repair guidance, and regression-test guidance into the submission-blocked Markdown draft; and render a `Studio A+B context` Markdown section without raw code snippets. `test_studio_report_candidate_guidance_includes_advisory_signal_labels` covers SARIF, SBOM, and fuzzing advisory labels for report review; `test_studio_candidates_include_imported_fuzzing_plan_context_as_advisory` asserts the exported JSON and Markdown include a fuzzing advisory section without leaking fuzzing filenames, bearer tokens, cookies, or secret values. `test_report_export_markdown_includes_candidate_summary_and_ranking_reasons`, `test_report_export_markdown_includes_report_readiness_next_action`, `test_report_export_markdown_includes_evidence_review_items`, `test_report_export_markdown_includes_provenance_review_items`, `test_report_export_markdown_includes_deduplication_review_items`, `test_report_export_markdown_includes_refutation_review_questions`, `test_report_export_markdown_includes_policy_review_items`, `test_report_export_markdown_includes_validation_review_items`, `test_report_export_markdown_includes_evidence_and_false_positive_checks`, `test_report_export_markdown_includes_evidence_gaps`, and `test_report_export_markdown_includes_validation_plan_and_safety_blockers` verify report-safe review sections while filtering secret-like items. | Extend only if future report drafts need richer per-artifact evidence-gap grouping. |
| Report draft is submission blocked | Complete | API source audit tests assert report preview `submission_blocked`; source audit payload records `report_draft.auto_submit_allowed == false`. | None for V0. |
| Scan persists as auditable pipeline run | Complete | Source audit API and repository tests assert pipeline run creation, artifact id, timeline, validation gate, and report preview; browser E2E submits the source-audit form and lands on a rendered run detail page. | None for V0. |
| Artifact provenance links scan, evidence bundle, and report claims | Complete | Source audit persistence tests assert artifact usage records for `pipeline_run`, `evidence_bundle`, and `report_claim`. | Add UI display checks if artifact detail becomes part of the V0 demo path. |
| Validation workspace is human gated | Complete | Tests assert `allowed_to_execute == false`, `human_approved == false`, claim tasks require review, and manual observations use report-safe evidence refs. | None for V0. |
| Finding candidate requires manual evidence and claim review | Complete | API test covers blocked promotion before manual observation and claim review, then successful candidate creation after both gates. Web helper smoke covers the same sequence without validation, approval, or submission calls; browser E2E verifies rendered report and validation pages keep submission and execution controls absent. | None for V0. |
| Report submission remains manual after candidate creation | Complete | API test asserts candidate promotion does not set auto submission; report draft keeps `auto_submit_allowed == false`. | No automatic submission endpoint should be added without a separate human-gated design. |
| Web source audit entrypoint is local and human gated | Complete | Web tests assert `/source-audit` posts only local repo/scope inputs and contains no validation, finding promotion, or submission actions. Browser E2E submits a mocked local source-audit scan and verifies the rendered run remains review-gated. | None for V0. |
| Web report preview records claim review manually | Complete | Web tests assert claim review form and API helper exist and avoid validation/submission actions; helper smoke sequences it after source audit and manual observation. Browser E2E verifies the rendered report page exposes the manual submission gate and no submit-report control. | None for V0. |
| Web validation workspace records safe manual observation | Complete | Web tests assert safe observation types, safety notes, no hardcoded unsafe observation path, and helper smoke sequences observation before claim review. Browser E2E verifies the rendered workspace shows the preflight gate and no execute/approve validation control. | None for V0. |
| Phase 3 refutation and ranking metadata is carried end-to-end | Complete for the current narrow slice | API tests assert `unverified`, `parked`, and `refuted` classification plus `false_positive_checks`, `priority_score`, and `ranking_reasons`; `test_source_hypotheses_rank_high_impact_sinks_before_lower_impact_sinks` covers impact-aware ranking reasons for destructive or privilege-sensitive sinks; Web tests assert run detail, report preview, and dashboard expose the state read-only. | Deeper semantic audit heuristics and browser-level confirmation remain outside this slice. |
| Service-layer authorization reduces false-positive hypotheses | Complete for the current narrow slice | `apps/api/tests/test_codebase_map.py` covers route-to-service authz mapping; `apps/api/tests/test_source_audit.py` covers `run_source_audit` not raising an authorization hypothesis when the called service performs ownership/role authorization before a sensitive sink. | Extend to aliasing and multi-hop service calls in later Phase 3 slices. |
| Multi-hop service ownership reduces false-positive hypotheses | Complete for the current narrow slice | `test_map_authorized_code_files_does_not_mark_gap_when_repository_layer_has_owner_filter` covers route-to-service-to-repository propagation where the repository applies an account boundary filter before the service reaches a sensitive sink; `test_map_authorized_code_files_follows_imported_service_alias_to_repository_owner_filter` covers imported service aliases on the route edge; `test_map_authorized_code_files_follows_local_method_alias_to_repository_owner_filter` and `test_map_authorized_code_files_follows_chained_local_alias_to_repository_owner_filter` cover local aliases to repository methods; `test_map_authorized_code_files_follows_same_class_field_alias_to_repository_owner_filter` covers same-class `self.loader = repository.load_for_user` aliases; `test_map_authorized_code_files_follows_chained_same_class_field_alias_to_repository_owner_filter` covers same-class `self.safe_loader = self.loader` aliases; matching source-audit tests cover the route-service-repository hypothesis result. | Extend to deeper or ambiguous call chains in later Phase 3 slices. |
| Multi-line service calls and membership filters preserve candidate coverage | Complete for the current narrow slice | `test_map_authorized_code_files_follows_multiline_repository_owner_filter` covers multi-line route-to-service-to-repository calls with a repository ownership filter and no authorization-gap candidate; `test_run_source_audit_raises_authorization_hypothesis_for_multiline_service_sink_without_authz` covers a multi-line route-to-service sensitive sink without authorization producing a traceable authorization hypothesis; `test_map_authorized_code_files_treats_multiline_membership_filter_as_authz_check` and `test_map_authorized_code_files_treats_bracketed_multiline_membership_filter_as_authz_check` cover split SQLAlchemy `.in_(...)` ownership filters, including bracketed value lists; `test_map_authorized_code_files_treats_multiline_kwarg_membership_filter_as_authz_check` covers split Django/ORM `account_id__in=[...]` ownership filters; matching source-audit tests cover hypothesis suppression for those multi-line membership forms. | Extend only when real authorized code shows additional multi-line call or ownership-filter forms. |
| Dependency-injected route authorization reduces false-positive hypotheses | Complete for the current narrow slice | `test_map_authorized_code_files_treats_dependency_injected_authz_as_route_authz` covers same-line FastAPI `Depends(require_user)` in handler signatures; `test_map_authorized_code_files_treats_keyword_dependency_authz_as_route_authz` covers keyword-form `Depends(dependency=require_user)` handler signatures; `test_map_authorized_code_files_treats_multiline_signature_dependency_authz_as_route_authz` covers multi-line handler signatures; `test_map_authorized_code_files_treats_decorator_dependency_authz_as_route_authz` covers same-line route decorator `dependencies=[Depends(require_user)]`; `test_map_authorized_code_files_treats_router_level_dependency_authz_as_route_authz`, `test_map_authorized_code_files_treats_qualified_router_level_dependency_authz_as_route_authz`, `test_map_authorized_code_files_treats_aliased_router_level_dependency_authz_as_route_authz`, `test_map_authorized_code_files_treats_multiline_router_level_dependency_with_prefix_as_route_authz`, and `test_map_authorized_code_files_treats_multiline_router_level_split_dependency_authz_as_route_authz` cover same-line and multi-line router default `APIRouter(...)`/`fastapi.APIRouter(...)`/aliased `APIRouter` dependencies; `test_map_authorized_code_files_treats_multiline_decorator_dependency_authz_as_route_authz` covers multi-line route decorators; `test_map_authorized_code_files_treats_decorator_dependency_wrapper_as_route_authz` and `test_map_authorized_code_files_treats_multiline_decorator_dependency_wrapper_as_route_authz` cover route decorators whose dependencies delegate through wrappers; `test_map_authorized_code_files_treats_security_dependency_authz_as_route_authz` and `test_map_authorized_code_files_treats_decorator_security_authz_as_route_authz` cover same-line `Security(require_user)` forms; `test_map_authorized_code_files_treats_multiline_scoped_security_as_route_authz` covers multi-line scoped `Security(...)` route dependencies; `test_map_authorized_code_files_treats_imported_authz_alias_as_route_authz` covers imported authz function aliases; `test_map_authorized_code_files_treats_dependency_alias_in_signature_as_route_authz`, `test_map_authorized_code_files_treats_keyword_dependency_alias_in_signature_as_route_authz`, and `test_map_authorized_code_files_treats_dependency_alias_in_decorator_as_route_authz` cover same-file dependency aliases; `test_map_authorized_code_files_treats_dependency_wrapper_as_route_authz` covers a cross-file dependency wrapper whose signature delegates to `Depends(require_user)`; `test_map_authorized_code_files_treats_dependency_wrapper_chain_as_route_authz` covers a two-hop dependency wrapper chain; `test_map_authorized_code_files_treats_deeper_dependency_wrapper_chain_as_route_authz` covers a three-hop wrapper chain; `test_map_authorized_code_files_treats_dependency_alias_to_wrapper_chain_as_route_authz` covers an imported dependency alias that points to a wrapper chain; matching source-audit tests cover the hypothesis result. | Extend to ambiguous dependency wrappers in later Phase 3 slices. |
| Flask route discovery and decorator authz improve candidate coverage | Complete for the current narrow slice | `test_map_authorized_code_files_marks_flask_sensitive_route_without_authz_as_gap_candidate` covers Flask `@app.route(..., methods=["GET"])` sensitive routes becoming traceable authorization-gap candidates; `test_map_authorized_code_files_treats_flask_login_required_as_route_authz` covers `@login_required` suppressing the gap; `test_map_authorized_code_files_marks_flask_add_url_rule_function_without_authz_as_gap_candidate` covers function views registered with `add_url_rule(...)`; `test_map_authorized_code_files_treats_flask_add_url_rule_function_decorator_as_route_authz` covers decorated function views; `test_map_authorized_code_files_marks_flask_method_view_without_authz_as_gap_candidate` covers `MethodView` plus `add_url_rule(...)`; `test_map_authorized_code_files_treats_flask_method_view_decorator_as_route_authz` covers class-level `decorators = [login_required]`; `test_map_authorized_code_files_treats_flask_method_view_tuple_decorator_as_route_authz` covers class-level `decorators = (login_required,)`; `test_map_authorized_code_files_treats_flask_method_view_method_decorator_as_route_authz` covers method-level `@login_required`; matching source-audit tests cover both hypothesis generation and suppression. | Extend to richer Flask method declarations only when real authorized code requires it. |
| Same-handler ownership filters reduce false-positive hypotheses | Complete for the current narrow slice | `apps/api/tests/test_codebase_map.py` covers same-handler owner, tenant, org, account, workspace, team, project, group, agent, user, and created-by boundary filters before sensitive sinks, including `created_by_id=current_user.id`, `created_by_id=current_user.pk`, `user.pk`, `request.user.id`, `request.user.pk`, `Document.workspace_id == current_user.workspace_id`, `InvitePolicy.team_id == current_user.team_id`, `ProjectExport.project_id == current_user.project_id`, `GroupExport.group_id == current_user.group_id`, `Agent.agent_id == current_user.agent_id`, `File.created_by == current_user`, `created_by=current_user`, `created_by__id=current_user.id`, `created_by_id__in=[current_user.id]`, relation membership, double-underscore relation ids, and SQLAlchemy `.in_(...)` membership variants; matching `apps/api/tests/test_source_audit.py` tests cover hypothesis suppression for the same ownership-filter families. | Extend to additional complex ORM variants and multi-hop service ownership predicates in later Phase 3 slices. |
| Agent tool authorization candidates are traceable | Complete for the current narrow slice | `test_map_authorized_code_files_marks_agent_tool_execution_without_authz_as_gap_candidate` and `test_run_source_audit_raises_authorization_hypothesis_for_agent_tool_execution_without_authz` cover `execute_agent_tool(...)` as a sensitive agent-tool sink that produces a traceable authorization hypothesis when no agent boundary is present; `test_map_authorized_code_files_marks_agent_tool_dispatch_without_authz_as_gap_candidate` and matching source-audit coverage cover `dispatch_agent_tool(...)` for queued or dispatch-style tool execution; `test_map_authorized_code_files_treats_agent_id_filter_as_authz_check` and matching source-audit coverage suppress the candidate when an `agent_id` boundary is present. | Extend only when additional real authorized agent-tool dispatch patterns require it. |
| Created-by kwarg ownership filters reduce false-positive hypotheses | Complete for the current narrow slice | `test_map_authorized_code_files_treats_created_by_id_current_user_id_kwarg_as_authz_check` covers Django/ORM-style `created_by_id=current_user.id` ownership filtering before a sensitive sink; `test_map_authorized_code_files_treats_created_by_id_current_user_pk_kwarg_as_authz_check` covers `created_by_id=current_user.pk`; `test_map_authorized_code_files_treats_created_by_id_user_pk_kwarg_as_authz_check` covers `created_by_id=user.pk`; `test_map_authorized_code_files_treats_created_by_id_request_user_id_kwarg_as_authz_check` covers `created_by_id=request.user.id`; `test_map_authorized_code_files_treats_created_by_id_request_user_pk_kwarg_as_authz_check` covers `created_by_id=request.user.pk`; `test_map_authorized_code_files_treats_created_by_relation_id_kwarg_as_authz_check` covers Django relation-id `created_by__id=current_user.id`; `test_map_authorized_code_files_treats_single_item_created_by_id_current_user_id_kwarg_membership_list_as_authz_check` covers `created_by_id__in=[current_user.id]`; matching source-audit tests cover the hypothesis suppression paths. | Extend only when additional real authorized code patterns require it. |
| Created-by relation ownership filters reduce false-positive hypotheses | Complete for the current narrow slice | `test_map_authorized_code_files_treats_created_by_current_user_comparison_as_authz_check` covers SQLAlchemy-style `File.created_by == current_user`; `test_map_authorized_code_files_treats_created_by_current_user_kwarg_as_authz_check` covers Django/ORM-style `created_by=current_user` ownership filtering before a sensitive sink; `test_map_authorized_code_files_treats_created_by_relation_id_kwarg_as_authz_check` covers Django/ORM-style `created_by__id=current_user.id`; matching source-audit tests cover these hypothesis suppression paths. | Extend only when additional real authorized code patterns require it. |
| Created-by membership ownership filters reduce false-positive hypotheses | Complete for the current narrow slice | `test_map_authorized_code_files_treats_single_item_created_by_id_current_user_id_membership_list_as_authz_check` covers SQLAlchemy-style `File.created_by_id.in_([current_user.id])` ownership filtering before a sensitive sink; `test_map_authorized_code_files_treats_single_item_created_by_id_current_user_id_kwarg_membership_list_as_authz_check` covers Django/ORM-style `created_by_id__in=[current_user.id]`; matching source-audit tests cover both hypothesis suppression paths. | Extend only when additional real authorized code patterns require it. |
| Browser-level V0 E2E | Complete for rendered smoke | `apps/web/e2e/v0-source-audit.spec.ts` uses Playwright to submit the source-audit form, land on run detail, open report preview, and open validation workspace with a mock source-audit API. | Full API+database browser E2E remains a later integration slice if needed. |

## Phase Status

| Phase | Current Status | Notes |
| --- | --- | --- |
| Phase 0: current-state and safety boundary freeze | Complete | The plan is documented and implementation uses existing FastAPI/Next/pipeline modules rather than a parallel architecture. |
| Phase 1: V0 local source audit MVP | Complete | CLI/API tests cover scope, intake, Semgrep, dependencies, hypotheses, and report output. |
| Phase 2: V0 quality gate and persistence | Complete for API, helper-level Web smoke, and rendered browser smoke | Pipeline run, artifact provenance, report preview, validation workspace, finding candidate gates, Web helper sequencing, and browser-rendered source-audit/report/workspace flow are covered by tests. Full API+database browser E2E remains outside this slice. |
| Phase 3: V0.5 semantic audit and refutation | In progress; refutation, ranking-quality, impact-aware ranking, service-layer authz, dependency-injected authz, Flask route discovery, and ownership-filter heuristic slices complete | Source hypotheses now carry `false_positive_checks`, `refutation_status`, `priority_score`, `ranking_reasons`, impact context, and sink-symbol summaries; API tests cover ranking `unverified` traceable candidates above parked/refuted candidates and privilege/destructive sinks above lower-impact data sinks; Web run detail, report preview, and dashboard expose the state read-only; source mapping detects Flask `@app.route(...)`, function-view `add_url_rule(...)`, and `MethodView`/`add_url_rule(...)` sensitive routes and suppresses class-level list/tuple, function-level, or method-level `@login_required` false positives; source mapping also suppresses the narrow route-to-service, route-service-repository ownership boundary including imported service aliases, local repository-method aliases, and same-class `self.<alias>` repository-method aliases including one-hop chained aliases, same-line and keyword-form FastAPI handler `Depends(require_user)`/`Depends(dependency=require_user)` plus multi-line FastAPI handler `Depends(require_user)`/`Security(require_user)`, same-line or multi-line route decorator `dependencies=[Depends(require_user)]`/`dependencies=[Security(require_user)]`, same-line and multi-line router default `APIRouter(...)`/`fastapi.APIRouter(...)`/aliased `APIRouter` dependencies, decorator-level dependencies that delegate through wrappers in same-line and multi-line forms, multi-line scoped `Security(...)` route dependencies, imported authz function aliases, same-file dependency alias, bounded cross-file dependency-wrapper authorization false positives through covered wrapper chains up to three hops, imported dependency aliases pointing to wrapper chains, and same-handler `owner_id == user_id`, `tenant_id == current_user.tenant_id`, `org_id == current_user.organization_id`, `org_id=current_user.organization.id`, `File.org_id == current_user.organization.id`, `File.account == current_user.account`, `File.owner == current_user`, `File.created_by_id == current_user.id`, `File.created_by == current_user`, `created_by_id=current_user.id`, `created_by_id=current_user.pk`, `created_by_id=user.pk`, `created_by_id=request.user.id`, `created_by_id=request.user.pk`, `created_by=current_user`, `created_by__id=current_user.id`, `File.created_by_id.in_([current_user.id])`, `created_by_id__in=[current_user.id]`, `filter_by(..., account_id=current_user.account_id)`, Django-style `account=current_user.account`, Django-style `owner=current_user`, Django-style `org=current_user.organization`, `account__in=current_user.accounts`, `account__id=current_user.account_id`, `account_id__in=current_user.account_ids`, `organization_id__in=current_user.org_ids`, `.in_([current_user.organization_id])`, `.in_([current_user.organization.id])`, `.in_((current_user.organization_id,))`, `.in_({current_user.organization_id})`, plus `.in_(current_user.tenant_ids)` and `.in_(current_user.accounts)` authorization filters. |
| Phase 4-7: V1/V2/V3/V4 | Plan/advisory only, with CRS/fuzzing candidate planning started | `tests/test_crs_fuzzing.py` covers parser, decoder, validator, BOM-prefixed parser, and protocol-handler candidate detection while asserting the fuzzer plan remains `not_executed`, command generation remains blocked, crash promotion requires human review, and network/destructive behavior stays disallowed. Existing modules may model these areas, but they should remain Scope Guarded, redacted, non-executing, and human-approved. |

## Recommended Next Slice

1. Continue Phase 3 with a deeper semantic-quality target:
   improve source-fact heuristics for deeper or ambiguous service call chains, additional complex ORM variants, ambiguous dependency wrappers, and richer impact reasoning while keeping outputs advisory.
2. Add a full API+database browser E2E only if rendered smoke is no longer enough for release confidence.
3. Keep all Phase 3 outputs advisory: no live validation, no automatic finding promotion, and no report submission without the existing manual gates.

## Current V0 Exit Criteria

V0 can be treated as API-complete when these commands pass:

- `python -m pytest` from `apps/api`
- `npm test` from `apps/web`
- `npm run lint` from `apps/web`
- `npm run build` from `apps/web`
- `npm run e2e` from `apps/web`

V0 can be treated as rendered-demo-ready after the Web smoke and browser E2E tests pass. Full API+database rendered-flow confidence remains a separate integration task.

## Latest Verified Web Slice

The refutation visibility and ranking slice was verified with:

- `npm test` from `apps/web`
- `npm run lint` from `apps/web`
- `npm run build` from `apps/web`
- `npm run e2e` from `apps/web`

These checks cover the Web helper smoke, source-audit refutation/ranking metadata typing, run detail display, report preview display, validation workspace display, browser-rendered V0 source-audit flow, and dashboard refutation summary. The API test suite also covers source-audit hypothesis ranking and propagation into finding JSON and Markdown reports.
