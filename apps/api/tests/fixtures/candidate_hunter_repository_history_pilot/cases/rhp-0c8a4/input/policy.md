# Offline benchmark policy

Analyze only the committed vulnerable source snapshot. Do not access the network,
execute against a live target, inspect the fixed snapshot, read the advisory, or
submit a report. Output is limited to a candidate hypothesis and a non-destructive
local validation plan.
